{
  "name": "docs",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "docker build -t slide-mermaid-builder .",
    "build-no-cache": "docker build -t slide-mermaid-builder . --no-cache",
    "convert": "docker run --rm -e TARGET_FILES=\"slides,slides-scope\" -v \"$(pwd)/src\":/app/src -v \"$(pwd)/dist\":/app/dist slide-mermaid-builder node convert.js",
    "dev": "npx http-server ."
  },
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^11.12.0"
  }
}
